dependencies {
    implementation 'org.springframework.ai:spring-ai-starter-mcp-server-webmvc'
}

// mcp-db-server는 라이브러리 모듈이므로 bootJar 생성 안함
tasks.named('bootJar') {
    enabled = false
}

// 일반 JAR 파일은 생성
tasks.named('jar') {
    enabled = true
}